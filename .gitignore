# Auto-generated by CleverCoder wizard (ct-2026-06-23-0033).
# git versiona solo el código; lo operacional y los secretos quedan fuera.
bin/
obj/
node_modules/
.vs/
*.user

# Operacional de CleverCoder — estado de la herramienta, no código.
.clevercoder/
.mcp.json

# Secretos — nunca a GitHub.
.env
.env.*
credentials/
secrets/
*.key
*.pem
*.pfx

# Datos personales — nunca a GitHub, ni siquiera dentro de docs/.
# Este repo se publica: lo que entra al historial queda público aunque después
# se borre. La evidencia de una prueba (volcados de chats, contactos, libretas,
# baselines con nombres reales) NO es código — va al scratchpad de la sesión o
# junto a la instalación. Ver constitution.md §2b.
docs/*BASELINE*
docs/*PARES*
docs/**/*.csv
*-contactos.*
*-chats.*
*-libreta.*
export-*.json
evidencia/

# Restos de la exploración de open-wa v5 (ct-2357, revocado) — perfiles de
# Chrome/Puppeteer, no código.
.open-wa/
_IGNORE_session/

# Binarios compilados (ct-2026-07-18-200550) — artefactos de build, no código.
*.exe
*.exe~

# Salida de build-all.sh (ct-2026-07-19-1414) — artefactos de build, no código.
dist/

# Python (adapters/display, ct-2026-07-19-1843) — bytecode cacheado, no código.
__pycache__/
*.pyc

# Datos personales — evidencia de prueba, no código (ct-2026-08-06, ver
# .clevercoder/constitution.md §2b). Vive en el scratchpad de la sesión,
# nunca en docs/.
docs/S13-C1-PARES.csv
docs/S13-C1-BASELINE.md
